version: '3.8'

services:
  db:
    image: mariadb
    container_name: mariaDB-uek
    ports:
      - '13306:3306'
    environment:
      MYSQL_ROOT_PASSWORD: "1"
    volumes:
      - ./db/:/var/lib/mysql
      - ./:/data/
    restart: unless-stopped